services:
  languagetool:
    image: erikvl87/languagetool
    ports:
      - "8010:8010"
    restart: unless-stopped
    # Optional: mount ngrams if the user provides them
    # volumes:
    #   - ./ngrams:/ngrams
