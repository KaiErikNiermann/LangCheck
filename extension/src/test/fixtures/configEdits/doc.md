# A document with one word that depends on the config

The preimage is accepted only while morphology is on, so it is the signal.

A recieve is the control: an ordinary misspelling, reported whatever the
config says, so a silent document means checking stopped rather than that the
config took effect.
