# A document for two engines and one severity

In order to proceed we need a large number of items, so the wordiness plugin
has something to say once it is switched on.

A recieve typo is the control, and the severity it is reported at is what a
`rules:` override changes.
