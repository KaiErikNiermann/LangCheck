# Only Vale's built-in checks, so this needs no style packages downloaded.
# `styles/` has to exist even when it is empty, or Vale refuses to start.
StylesPath = styles
MinAlertLevel = suggestion

[*.md]
Vale.Repetition = YES
