# A document for the optional engines

This is is a repeated word, which Vale's built-in Repetition check reports
once Vale is switched on.

A recieve typo is the control, reported by Harper whatever else is running.
