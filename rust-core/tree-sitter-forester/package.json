{
  "name": "tree-sitter-forester",
  "version": "0.1.0",
  "private": true,
  "main": "grammar.js",
  "dependencies": {
    "tree-sitter-cli": "^0.25.0"
  },
  "pnpm": {
    "// onlyBuiltDependencies": "tree-sitter-cli fetches its binary in a postinstall script; without this pnpm blocks it and `tree-sitter generate` cannot run.",
    "onlyBuiltDependencies": ["tree-sitter-cli"]
  },
  "tree-sitter": [
    {
      "scope": "source.forester",
      "file-types": ["tree"]
    }
  ]
}
